@model IEnumerable<FundAppsCourierKata.Models.Parcel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ParcelSize)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Dimension)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.deliveryCost)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ParcelSize)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Dimension)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.deliveryCost)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ParcelId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ParcelId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ParcelId })
        </td>
    </tr>
}

</table>
